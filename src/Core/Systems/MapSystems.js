/**
 * Esse sistema é responsável por gerar um mapa randomico com a altura (height)
 * e largura (width) informados.
 * @param {Object} entity Entidade que o sistema vai trabalhar
 * @param {number} width Largura do mapa
 * @param {number} height Altura do mapa
 */
export function generateMap(entity, width, height) {}
